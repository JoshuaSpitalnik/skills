# Index: <!-- FILL: key → what it resolves to, e.g. "queue name → owning consumer" -->

<!-- FILL: one or two sentences on when someone arrives here - specifically, what they have in
     hand. An index is defined by its key: if you cannot state exactly what someone is holding
     when they open this file, the index has no entry point and will not be used. -->

Rows point; they do not explain. Service facts live in
[`../../systems/docs/`](../../systems/docs/) and fixes live in runbooks - an index row that
holds either will contradict the source within a quarter, and nobody will know which is right.

Sorted by: <!-- FILL: and say why - alphabetical for scanning, by frequency for speed -->

---

| <!-- FILL: the key --> | <!-- FILL --> | <!-- FILL --> | <!-- FILL --> |
|---|---|---|---|
| <!-- FILL --> | | | |
|  |  |  |  |

---

## Column definitions

<!-- FILL: what belongs in each column and, for the one that carries the most value, what a
     good entry looks like versus a useless one. Every index has one column that does the work;
     name it and show the difference. -->

## Filling this in

<!-- FILL: where the entries come from - an existing config, a tool's output, or accumulated
     experience. If they can be generated or reconciled automatically, say how; a hand-
     maintained index of something the machine already knows will drift. -->

## Maintenance

<!-- FILL: the trigger that adds a row. "After every incident that used this index" is a good
     one because it is tied to an event that actually happens. "Quarterly review" is not,
     because it is tied to nobody's calendar. -->
